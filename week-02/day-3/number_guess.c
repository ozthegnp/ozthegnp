#include <stdio.h>
#include <stdlib.h>include

void main()
{
    int r = rand() % 100;
    int guess = 0;
    scanf("%d", &guess);

    do {
            if (guess > r){
                printf();
            }

    } while (guess != r)
}
