#include <stdio.h>
#include <stdlib.h>
#include "sum.h"

// Write a function called `sum` that sum all the numbers
// until the given parameter which you store in a .h file
int main() {

    printf("\nThe numbers added up until the parameter is: %d\n", sum(6));

    return 0;
}
