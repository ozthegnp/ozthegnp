#include <stdio.h>

int main() {

    for(int a = 0 ; a <= 20; a++) {
        if (a % 2 == 0) {
        printf(" %d \n", a);
        }
    }
	// print the even numbers till 20
	return 0;
}
